import React from 'react';

const CopyUrlButton = () => {
    return (
        <button
            type='button'
            className='btn btn-info w-100 copy-url-button'
        >Copy GIF</button>
    );
};

export default CopyUrlButton;