import React from 'react';

const SearchButton = () => {
    return (
        <div className='col-4'>
            <button
                className='btn btn-info w-100 h-100'
            >Search</button>
        </div>
    );
};

export default SearchButton;